<%= simple_form_for([flat, review],
  data: {
    insert_in_list_target: "form",
    action: "submit->insert-in-list#send"
  }) do |f|
%>
  <%= f.input :title %>
  <%= f.input :rating %>
  <%= f.input :content %>
  <%= f.button :submit %>
<% end %>
